---
name: gzh-design
description: 微信公众号文章排版引擎，将 Markdown 转换为可直接粘贴到公众号编辑器的 HTML。内置蓝橙简约主题（主色 #1B5BEE 蓝/#D97706 橙/#4A3728 棕，章节标题用蓝底白字矩形块，引言框用双色边框）。支持 Markdown / Word(.docx) / PDF / 纯文本输入，自动章节编号、引言卡片、目录导航、代码块、图片/GIF、作者签名。也支持根据用户描述/参考图生成自定义主题组件库并保存复用。
---

# 公众号文章排版 Skill（蓝橙简约定制版）

把一篇 Markdown 文章转换为可直接复制粘贴进微信公众号编辑器、且粘贴后样式不丢失的 HTML。

核心资产是 `references/theme-blue-warm.md`（蓝橙简约主题组件库：设计变量 + 各组件完整 HTML + 模板骨架 + 映射规则）外加 `references/common-components.md` 通用增量库（代码块 / 图片·GIF / 小标签标题）。本 SKILL.md 只负责流程与决策，**具体 HTML 代码一律从组件库取，不要凭记忆手写**。

## 工作流

### 0. 输入与格式归一化

用户可能给：Markdown 文本或 `.md` 路径（直接进第 1 步）、`.docx`、`.pdf`、`.txt`/无标记纯文本、网页富文本。**非 Markdown 输入必须先读 `references/format-normalize.md`** 按其规则转成 Markdown 草稿并做结构确认（docx 用 `scripts/extract_docx.py`，PDF 用 Read 分页读取+清噪，纯文本按标题启发式推断结构）。

用户说「直接排 / 自动排 / 一键 / 不用问」时进**全自动模式**：跳过结构确认，自动推断结构、自选主题、排版校验，交付时附决策说明。

### 1. 选主题（自动使用蓝橙简约）

本版本内置唯一的「蓝橙简约」主题，无需选择。直接使用 `references/theme-blue-warm.md` 作为组件库。如需全新风格，走「自定义主题生成」流程（见下节）。

### 2. 读组件库

(1) 读主题专属组件库 `references/theme-blue-warm.md`（含封面、章节标题、正文标记、引用、签名等所有组件）。
(2) **同时读通用增量库** `references/common-components.md`——代码块、图片/GIF、小标签标题，套用蓝橙简约主题色。

后续生成完全依据这两份组件库，HTML 一律从中取、不要手写。

### 3. 解析 Markdown 结构

| 元素 | 识别规则 |
|------|---------|
| 文章标题 | `# 标题` 或 frontmatter `title` |
| 开头引言 | 文章最开头的 `> 引用` 块 |
| 章节标题 | `## 标题` |
| 子章节 | `### 标题` |
| 加粗 / 高亮 / 下划线 | `**文字**` / `==文字==` / `<u>文字</u>` 或 `++文字++` |
| 引用段落 | 非开头的 `> 文字` |
| 图片 / GIF | `![说明](URL)`、`![](xxx.gif)` |
| 代码 / 命令 / Prompt | ` ``` 围栏代码块 ``` `、行内 `` `code` `` |
| 分割线 / 列表 | `---`、`***` / `- 项` 或 `1. 项` |
| 表格 | `\|` 分隔的 Markdown 表格 |

**解析完结构后，判定文章类型**（取主导类型，可复合）：教程/操作指南、盘点/工具清单、观点/深度分析、访谈/人物特稿、数据复盘/报告、生活/情感随笔、案例实战。

### 4. 按配方选组件组合 → 装配 HTML

**先查主题库的「文章类型 → 组件组合配方」表**，按文章类型确定本篇的核心组件组合与点缀组件。

然后依主题库的**「完整文章模板骨架」章节**装配，把每个 Markdown 元素替换为对应组件：

- **骨架顺序以主题库为准**。
- **行内标记按语义到组件库里找对应组件**：`**加粗**`→蓝色加粗；`==高亮==`→橙色渐变高亮；`<u>文字</u>` 或 `++文字++`→橙色下划线；`~~文字~~`→荧光笔；`>引用`→蓝色竖条引用块。
- 来自**通用增量库**的组件：` ``` 代码块 ``` `→ 1a 深色 / 1b 浅色代码块，行内 `` `code` ``→ 1c；`![说明](url)`→ 2a 图片、`.gif`→ 2b GIF；小标题/强调→ 3a 左竖条小标题或 3b 药丸标签。
- **优先级：先查主题库映射规则表**——该主题有等价语义组件就用主题库版本；没有时才用通用库。

### 5. 校验合规（强制）

把生成的 HTML 写入目标文件后，**必须运行校验脚本**，ERROR 清零才算完成：

```bash
<SKILL_ROOT>/scripts/validate_gzh_html.py <生成的.html 的实际路径>
```

它确定性地检查平台禁用项和 `<span leaf>` 包裹。报 ERROR 就回到第 4 步修；**半角标点 WARNING 同样要修复到 0 再交付**。

### 6. 输出

**产物格式：纯 `<section>…</section>` 正文片段，不要包 `<!DOCTYPE>`/`<html>`/`<head>`/`<body>`**。

1. **干净正文文件**：HTML 保存到当前工作目录，文件名 `{原文件名}_排版_蓝橙简约(blue-warm).html`。
2. **带「复制」按钮的预览页**（让用户一键复制）：
   ```bash
   <SKILL_ROOT>/scripts/wrap_preview.py <上面的干净正文.html>
   ```
   产出 `{...}_预览.html`——浏览器打开后右上角有「复制到公众号」按钮。
3. 告知用户：**打开预览页 → 点右上角「复制」→ 公众号编辑器粘贴**。

## 生成时的智能处理

1. **章节自动编号**：按 `##` 出现顺序分配 `01/02/03…`；末章若为结语/总结类用 `///` 编号变体，PART 改 `LAST`。
2. **蓝底标题**：章节标题用组件4 蓝底矩形块，编号 01/02/03，末章用 ///。英文标签（TUTORIAL等）可选项。
3. **正文关键词下划线**：对**全文**主动找出不超过 5 处最重要的关键短语（宁可少标不要过标），用主题的蓝色下划线标记。优先标核心观点、结论、关键数据、专有名词。
4. **开头引言卡**：文章开头的 ">" 引言块用组件 9a 双色边框框，带蓝色引号装饰，左侧蓝框 #329DFC + 右侧暖橙框 #FCDCB1。引言内的署名统一用 "—— 作者名" 格式。
5. **目录提取**：从所有 `##` 取前 3 个作为导读/目录要点。
6. **开头引言卡署名**：按文章作者或主题而定——文章有署名就写"—— 作者名"，没有就用与主题相关的简短落款。
7. **尾部作者签名区**：默认用 `{{作者名}}` 占位让用户替换；原文末尾已有签名段则直接沿用。
8. **列表转换**：按主题库映射规则处理；无专属列表组件时转为带缩进的正文段落。
9. **中文全角标点**：正文标点一律用全角，代码块/行内代码/英文专名/URL 内部保持原样。

## 视觉层级（3 层递进，蓝橙简约通用）

| 层级 | 作用 | 频率 | 手段 |
|------|------|------|------|
| 锚点层 | 最强锚点：产品名/步骤/CTA/核心金句 | 全文 ≤ 5 处 | 蓝色加粗 6a、双色引言框 9a
| 标记层 | 蓝色下划线 6e（默认）| 全文 ≤5 处关键点 | 仅标关键数据/结论 |
| 容器层 | 引用块、概念标签、提示信息 | 按需 | 双色框 9a、蓝竖条 9a-alt、亮点卡 9b、提示 10x

## 平台红线（核心，完整检查交给校验脚本）

- **禁止**：`<style>`/`<script>`/`<div>`、`class`/`id` 属性、`position:fixed/absolute/sticky`、`float`、`@media`/`@keyframes`、`display:grid`、CSS 变量、外部字体/CSS。
- **必须**：样式全部内联 `style`；所有文字节点用 `<span leaf="">文字</span>` 包裹。
- **可用**：`display:flex`（有限）、`linear-gradient`、`border-radius`、`box-shadow`、`<section>/<p>/<span>/<strong>/<img>/<h3>`。

## Gotchas

- **漏 `<span leaf>` 包裹**是最常见致命错——靠校验脚本兜底，别跳过。
- **下划线：全文 ≤5 处，只标最关键的数据/结论**。宁可少标不要过标。不要有的段落密集有的段落空白。
- **章节编号错乱**：严格按 `##` 顺序，不要跳号。
- **签名区有且仅有末尾一个**：原文末尾已有作者签名则并入，不重复。
- **图片说明硬造**：只有 `![说明](url)` 里真有说明文字才生成说明组件。
- **图片自适应**：`<img>` 一律 `max-width:100%;height:auto;display:block;margin:0 auto`。
- **正文蓝色做主色**：锚点层全文 ≤5 处，橙色点缀不超过 1/5 段落。
- **代码块要紧凑**：每行一个 `<p style="margin:0">`，不用 `white-space:pre`。
- **待补素材居中**：用通用库 2c 居中板块，不是左对齐提示块。

## 自定义主题生成（第二条工作流）

用户想要蓝橙简约之外的风格时，**读 `references/theme-generator.md` 并严格按其流程执行**：

1. **收集偏好**：主题描述必填（或参考图），一次问全不逐字段追问。
2. **生成区块库 HTML**：产出 45~75 个 Block 的完整区块库，保存到 `assets/theme-previews/{theme-id}.html`——用户整页一次浏览确认风格。
3. **转换 + 登记**：用户确认后转换为标准 `references/theme-{标识}.md`（补 `<span leaf="">`、去掉预览用 id、补齐五章节），登记 theme-index.md，跑 `component_lint.py` 到 0 ERROR。
4. 交付后该主题即成为常驻可选主题。

## 添加新主题的规范

新主题以 `references/theme-{英文标识}.md` 命名，内容必须包含：
1. 设计变量速查表
2. 各组件完整 HTML（内联样式 + `<span leaf="">` 包裹，遵守"平台红线"）
3. 完整文章模板骨架
4. 文章类型 → 组件组合配方表
5. Markdown → 组件映射规则表

添加后在 `references/theme-index.md` 登记一行，并跑 `python3 scripts/component_lint.py .` 确认组件库无反模式（0 ERROR）。
